default:
    autoload: [ %paths.base%/test/contexts ]
    suites:
        default:
            paths: [ %paths.base%/test/features ]
            contexts:
                - FeatureContext:
                    session:   '@session'
    extensions:
        Behat\Symfony2Extension: ~
